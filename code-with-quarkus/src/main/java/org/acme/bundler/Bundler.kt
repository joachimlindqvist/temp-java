package org.acme.bundler

//import
//import org.acme.bundler.types.Recordable
//import kotlin.collections.List

class BundlerKt(
    val input: String?
) {

    fun a(): String {
        val fn = {};
        val a: String = "";
//        val b = Bundler(listOf(Recordable));

        val e = input?.length
        return e.toString()
    }
}

fun Int.square(): Int {
    return this * this;
}